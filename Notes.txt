- No types in solidity assembly 
- Assembly just knows about memory slots and op codes 
- Can't use normal arithmetic operators like + (have to use op codes like "add")
- Use let keyword to define variable and ":=" to assign value 
- Cannot access state (contract storage) variables in assembly 